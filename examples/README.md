# Example Data

This folder contains small mock input files used to demonstrate the expected format of the GenomeMarker workflow.

The file `example_alignment.fasta` is a synthetic aligned FASTA example. It is not derived from real unpublished research data and should only be used for demonstration purposes.

Expected outputs can be placed in `expected_output/` in future versions of the project.
